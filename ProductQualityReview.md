# 产品质量审计

> 审计日期：2026-07-30。该文档将“上万种用户”收敛为可组合的状态空间，用于决定改动优先级与发布验收。准出规则以 [QualityGate.md](QualityGate.md) 为准。

## 结论

当前最重要的质量目标不是继续增加页面，而是保证用户每一步都能得到真实、可解释、可恢复的下一步。

## 已修复的高优先级问题

| 用户情境 | 原风险 | 当前处理 |
| --- | --- | --- |
| 上传未知格式或损坏文件 | 二进制可能被误读为文本并送入模型。 | 前端限制格式和 12 MB；本地与 Netlify 提取服务统一返回 `415` 或 `413`。 |
| 简历文件为空或无法提取 | 仍可能请求模型并产生不可核验草稿。 | 简历生成要求至少 40 个字符的原文；不足时不生成、不保存、不导出。 |
| 聊天附件解析失败 | 空附件可能被悄悄传给模型，用户误以为已阅读。 | 解析失败会阻断消息发送并列出失败文件。 |
| 异常注册输入 | 无效邮箱可能进入本地开发数据。 | 本地与生产均校验邮箱格式和 8-128 位密码。 |
| 发布仅依赖页面可打开 | 类型、鉴权和关键边界可在上线后才暴露。 | Netlify 构建强制执行 `npm run test:release`。 |

## 体验优化优先级

### P1：下一轮必须解决

1. **密码与会话安全**：当前密码仍使用 SHA-256，缺少找回密码、限流、刷新令牌与正式会话撤销。生产开放注册前必须迁移到慢哈希和服务端会话策略。
2. **文件对象存储**：当前云端仅保存结构化结果和文件名，原文件不会保存；跨设备重新生成仍需用户重新上传原简历。
3. **AI 输出语义校验**：简历已有来源引用和数字校验，但还不能识别“参与”被强化为“主导”一类语义夸大。需要先设计可解释的逐句核验模型。
4. **任务恢复**：面试报告可恢复，但进行中的题目、计时和未提交回答刷新后会丢失。
5. **性能预算**：主包约 1.13 MB，语音转写依赖约 825 KB；PDF/OCR/Whisper 应继续按路由与动作延迟加载。

### P2：证明需求后再做

1. 真实职位数据、职位收藏与投递跟踪。
2. 视频数字人。
3. 手机号/微信登录。后端草案存在，但当前产品不应宣传为可用能力。
4. 管理员后台与 CRM。需要独立权限、审计和数据生命周期设计。

## 用户组合覆盖

用六个独立维度组合覆盖用户行为，而不是尝试枚举每一个人：身份、路径状态、材料质量、AI 状态、设备能力、网络与持久化。

每个变更至少选择以下四种组合进行验收：

1. 正常闭环：登录用户、真实 JD、可解析简历、AI 正常、桌面端。
2. 前置缺失：游客或未选择岗位、无 JD、无简历。
3. 失败降级：AI 不可用、文件无法解析、设备权限被拒绝或云端保存失败。
4. 受限环境：390px 移动端、无摄像头或无系统女声。

## 本轮证据

- `npm run test:release` 通过：严格 TypeScript、6 项自动化测试、生产构建和差异检查。
- 本地 API：未知文件格式返回 `415`，无效邮箱返回 `400`。
- 390px：首页、测评、简历、面试、规划师均无横向溢出。
- 已知残余：完整 ESLint 基线仍含历史问题，暂不作为发布阻断项；构建仍报告大包和第三方 `eval` 告警。
