# 发布准出机制

> 本文件定义职向未来 Pro 的发布门槛。任何功能改动都必须通过对应层级；不满足阻断项，不得合并或部署。

## 1. 自动阻断门槛

Netlify 使用 `npm run test:release` 作为构建命令，依次执行：

1. `npm run check:types`：前端严格 TypeScript 检查。
2. `npm test`：认证、越权、注册输入与文件边界测试。
3. `npm run build`：生产构建。
4. `git diff --check`：拒绝空白错误。

任一步失败，Netlify 构建必须失败，不能发布生产版本。历史 ESLint 基线尚未清零，因此当前不将 `npm run lint` 作为发布阻断项；修复后才可加入门禁。

## 2. 场景矩阵

“上万种用户”由下列正交变量组合覆盖：

| 变量 | 覆盖值 |
| --- | --- |
| 身份 | 游客、新注册、已登录、令牌过期、跨用户令牌 |
| 路径状态 | 无测评、测评未完成、已选岗位、无真实 JD、有真实 JD、历史草稿 |
| 材料 | 粘贴文本、PDF、DOC/DOCX、扫描件、空文件、加密/无法解析文件、超限文件 |
| AI 状态 | 成功、超时、无效 JSON、包含未证实事实、题库失败、本地回退 |
| 设备 | 无摄像头、拒绝麦克风、无 SpeechRecognition、无女声、移动窄屏 |
| 网络与存储 | 在线、接口失败、云端写失败、刷新、跨设备恢复 |

组合原则：每次改动至少覆盖一条正常路径、一条前置条件缺失路径、一条失败降级路径和一条移动端路径。

## 3. 核心人工验收

发布候选版本必须在生产站点完成下列验证：

1. 游客从首页进入测评，开始任务时登录并返回原任务。
2. 已登录用户完成测评，报告只输出方向假设、当前信号、差距和行动，不输出录用预测或伪评分。
3. 从岗位页选择方向后，简历页必须要求真实 JD；无真实 JD、空文本或无法提取文件时不得生成或导出。
4. 简历 AI 输出包含占位符、未在原文出现的数字或缺失来源引用时，必须被拦截。
5. 面试必须在题库准备结束后才计时；拒绝设备权限时仍可文字作答；题目在单轮训练内不变化。
6. AI 规划师上传失败附件时必须显示失败，不得悄悄把空附件送给模型；成功附件不能原文回显到用户提示词。
7. 390px 宽度下首页、测评、简历、面试和规划师无横向滚动；聊天只在消息区滚动。
8. 登录用户刷新后可恢复职业报告、岗位、合格简历草稿、会话和最近面试报告；跨用户接口返回 `403`。

## 4. 阻断规则

以下任一情况属于 P0，必须阻断发布：

- 未授权访问、跨用户读取/写入、令牌校验失效。
- 简历生成、复制或 Word 导出包含无法追溯的候选人事实。
- 将岗位策略描述为真实 JD、将测评信号描述为能力证明或录用预测。
- 文件解析失败后仍把乱码、空内容或未解析附件传入模型。
- 关键主路径无法完成且没有明确失败状态或可行动替代路径。

P1 可以发布但必须建档：浏览器女声音色差异、OCR 质量波动、外部 AI 短暂不可用、历史草稿需要重新生成。
